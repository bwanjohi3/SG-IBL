var joi = require('joi');

module.exports = {
    description: 'list card cdol1 profiles',
    notes: ['list card cdol1 profiles'],
    tags: ['card', 'cdol1', 'profile', 'list'],
    params: joi.any(),
    result: joi.any()
};
