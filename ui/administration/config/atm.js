export const atmConfig = {
    atm: {
        terminals: {
            grid: {
                vissibleFields: ['terminalId', 'terminalName', 'identificationCode', 'customization']
            }
        }
    }
};
